'use client'
import React from 'react';
import ThemeComponent from './ThemeComponent';

const ThemeComponentClient = (props) => {
  return <ThemeComponent {...props} />;
};

export default ThemeComponentClient;
